name: Manual Trigger
on:
  workflow_dispatch:
    inputs:
      name:
        description: "Enter your name"
        required: true
        default: "Eswar"

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greeting
        run: echo "Hello, ${{ github.event.inputs.name }}!"
